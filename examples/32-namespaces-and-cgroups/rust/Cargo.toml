[package]
name = "pmon"
version = "6.0.0"
edition = "2024"

[[bin]]
name = "app"
path = "src/main.rs"

[dependencies]
nix = { version = "0.31.3", features = ["sched", "mount", "hostname", "process", "fs", "signal", "user"] }
