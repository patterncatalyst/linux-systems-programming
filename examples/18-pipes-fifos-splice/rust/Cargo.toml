[package]
name = "pmon"
version = "0.4.0"
edition = "2024"

[[bin]]
name = "app"
path = "src/main.rs"

[dependencies]
anyhow = "1"
nix = { version = "0.31", features = ["event", "signal", "process", "fs"] }
rustix = { version = "1", features = ["event", "fs", "pipe", "process"] }
