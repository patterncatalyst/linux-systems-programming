[package]
name = "chatterd-fastpath"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "app"
path = "src/main.rs"

[dependencies]
nix = { version = "0.31", features = ["socket", "net", "poll", "sched", "signal", "fs"] }
